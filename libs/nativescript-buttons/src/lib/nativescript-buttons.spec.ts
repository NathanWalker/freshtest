import { nativescriptButtons } from './nativescript-buttons';

describe('nativescriptButtons', () => {
  it('should work', () => {
    expect(nativescriptButtons()).toEqual('nativescript-buttons');
  });
});
